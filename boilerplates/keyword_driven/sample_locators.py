# Sample locators for Keyword Driven Framework
from selenium.webdriver.common.by import By

class Locators:
    LOGIN_BUTTON = (By.ID, 'login-button')
    USERNAME_FIELD = (By.ID, 'username')
    PASSWORD_FIELD = (By.ID, 'password')
